# Launch Checklist

✅ Replace `.env` secrets with production values  
✅ Configure S3 or MinIO bucket and CORS rules  
✅ Optional: integrate Google Vision OCR (set OCR_USE_GOOGLE=true)  
✅ Set up SendGrid for email reminders  
✅ Optional: configure Stripe for Premium plans  
✅ Deploy backend (Render, Railway, or Docker)  
✅ Deploy frontend (Vercel or Netlify)  
✅ Add custom domain (e.g. warrantyvault.in)  
✅ Add Privacy Policy and Terms (below)  
✅ Test uploading, parsing, and expiry reminders  
✅ Announce beta launch 🎉
