# WarrantyVault — Full Release (Demo)

This repository contains a local-demo-ready version of WarrantyVault:
- Frontend: Vite + React (simple Upload + Dashboard)
- Backend: Node + Express (signed upload + Tesseract OCR demo)
- Local demo stack supported: Docker Compose (Postgres + MinIO + backend + frontend)
- `RUN_THIS_FIRST.txt` contains step-by-step instructions for non-coders.

Important: This is a demo. Replace secrets and enable production providers (AWS S3, Google Vision, SendGrid, Stripe) before launching.
